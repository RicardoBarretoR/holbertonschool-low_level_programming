#include "holberton.h"
/**
 *_abs - function that computes the absolute value of an integer
 *@i: number to calculate the absolute value
 *Return: 0
 */
int _abs(int i)
{
	if (i < 0)
	{
		i = i * -1;
	}
	else
	{
	}
	return (i);
}
